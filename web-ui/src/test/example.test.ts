// @group UnitTests : Example vitest smoke test — delete and replace with real tests

import { describe, it, expect } from 'vitest'

describe('vitest setup', () => {
  it('is working', () => {
    expect(1 + 1).toBe(2)
  })
})
